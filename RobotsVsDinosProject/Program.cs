using System;
using System.Dynamic;

namespace RobotsVsDinosProject
{
    class Program
    {
        static void Main(string[] args)
        {
            Battlefield battlefield = new Battlefield();
            battlefield.FullBattleMethod();
            //if robotFleet & dinoHerd already exist in battlefield class, don't need to pass in parameters (these already exist once we instantiate battlefield class)


        }   
    }
}
